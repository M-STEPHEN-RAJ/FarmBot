import React from 'react'

export const metadata = {
  title: "FarmBot | Seller",
  icons: {
    icon: "/images/seller/login/logo.png",
  },
};

const SellerLayout = ({ children }) => {
  return (
    <div>{children}</div>
  )
}

export default SellerLayout